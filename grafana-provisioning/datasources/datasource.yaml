apiVersion: 1

datasources:
  - name: Crawler Data
    type: postgres
    url: postgres:5432
    database: crawler
    user: postgres
    password: postgres
    secureJsonData:
      password: postgres
    jsonData:
      sslmode: disable